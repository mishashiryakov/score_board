type Props = {
  homeName: string;
  awayName: string;
  homeScore: number;
  awayScore: number;
};

export const GameCard = ({
  homeName,
  awayName,
  homeScore,
  awayScore,
}: Props) => {
  return <div></div>;
};
